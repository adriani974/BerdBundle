{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ResultFields</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fieldname</th>
                <td>{{ entity.fieldName }}</td>
            </tr>
            <tr>
                <th>Fieldvalue</th>
                <td>{{ entity.fieldValue }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ entity.userId }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('resultfields') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('resultfields_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
