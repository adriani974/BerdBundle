{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Requete list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Body</th>
                <th>Requestname</th>
                <th>Isenable</th>
                <th>Isfixed</th>
                <th>Rendertype</th>
                <th>Userid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('requete_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.body }}</td>
                <td>{{ entity.requestName }}</td>
                <td>{{ entity.isEnable }}</td>
                <td>{{ entity.isFixed }}</td>
                <td>{{ entity.renderType }}</td>
                <td>{{ entity.userId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('requete_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('requete_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('requete_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
