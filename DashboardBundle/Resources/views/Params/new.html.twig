{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Params creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('params') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
